{
  "name": "hook-marketplace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm --recursive run test",
    "typecheck": "pnpm --recursive run typecheck",
    "lint": "pnpm --recursive run lint",
    "build": "pnpm --recursive run build",
    "build-index": "tsx registry/scripts/build-index.ts",
    "validate-all": "tsx registry/scripts/validate-all.ts",
    "build-archives": "tsx registry/scripts/build-archives.ts"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
