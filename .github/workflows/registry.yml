name: Registry Validation

on:
  pull_request:
    paths:
      - 'registry/hooks/**'
      - 'registry/scripts/**'
      - 'packages/schema/src/**'

jobs:
  validate:
    name: Validate & Rebuild Index
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v3
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate all hook.json files
        run: pnpm run validate-all

      - name: Rebuild index.json
        run: pnpm run build-index

      - name: Check index.json is up to date
        run: |
          if ! git diff --quiet registry/index.json; then
            echo "registry/index.json is out of date — run 'pnpm run build-index' and commit the result"
            git diff registry/index.json
            exit 1
          fi
